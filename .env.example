### OpenAI (LIVE tests / pipeline)
OPENAI_API_KEY=sk-...

### Supabase (prompt repository)
# Project URL (Settings → API → Project URL)
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co

# Server-side key (Settings → API → service_role key) recommended for backend usage.
# Keep it secret; do NOT ship it to frontend.
SUPABASE_KEY=YOUR_SUPABASE_SERVICE_ROLE_KEY

### Optional: external secrets directory (env-first, then files)
# If omitted, defaults to: ../secrets (one level above repo root)
# Secrets can be provided as:
# - a single file: ../secrets/.env (recommended)
# - or per-secret files: ../secrets/OPENAI_API_KEY, ../secrets/SUPABASE_URL, ...
AI_DOMAIN_SECRETS_DIR=/absolute/path/to/secrets
